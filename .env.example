# Comma-separated list of origins allowed to call this service.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,http://localhost:7000

# Host and port for Uvicorn (used in the run command below)
HOST=0.0.0.0
PORT=8000
